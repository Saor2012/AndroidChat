<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android" >
    <data>
        <variable
            name="event"
            type="com.example.androidchatclient.presentation.fragment.chat.IChatPresenter.Presenter" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/item_box"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="40dp"
        android:layout_marginTop="0dp"
        android:layout_marginBottom="2dp"
        android:background="@drawable/message_box">

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/item_message"
            android:layout_width="408dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="2dp"
            android:layout_marginEnd="2dp"
            android:maxLength="680"
            android:minHeight="18dp"
            android:paddingStart="@dimen/menu_padding_text"
            android:paddingEnd="@dimen/menu_padding_text"
            android:text=""
            android:hint="@string/fragment_chat_hint_login"
            android:textSize="@dimen/menu_size_msg"
            android:textColor="@color/colorBlack"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.492"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/item_login" />

        <androidx.constraintlayout.widget.Guideline
            android:id="@+id/guideLine"
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginTop="18dp"
            android:orientation="horizontal"
            app:layout_constraintGuide_begin="20dp" />

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/item_login"
            android:layout_width="308dp"
            android:layout_height="18dp"
            android:layout_marginStart="2dp"
            android:layout_marginTop="2dp"
            android:paddingStart="@dimen/menu_padding_text"
            android:paddingEnd="@dimen/menu_padding_text"
            android:text=""
            android:textColor="@color/colorBlack"
            android:hint="@string/fragment_chat_hint_login"
            android:textSize="@dimen/menu_size_text"
            app:layout_constraintBottom_toTopOf="@id/guideLine"
            app:layout_constraintEnd_toStartOf="@+id/item_date"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/item_date"
            android:layout_width="0dp"
            android:layout_height="18dp"
            android:layout_marginTop="2dp"
            android:layout_marginEnd="2dp"
            android:paddingStart="@dimen/menu_padding_text"
            android:paddingEnd="@dimen/menu_padding_text"
            android:text=""
            android:textColor="@color/colorBlack"
            android:hint="@string/fragment_chat_hint_date"
            android:textSize="@dimen/menu_size_text"
            app:layout_constraintBottom_toTopOf="@id/guideLine"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@+id/item_login"
            app:layout_constraintTop_toTopOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
